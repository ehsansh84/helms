apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  routes:
    - kind: Rule
      match: Host(`argo.devserver.ir`)
      services:
        - kind: Service
          name: argocd-demo-server
          namespace: argocd
          port: 80
