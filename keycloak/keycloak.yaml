service:
  type: ClusterIP
postgresql:
  enabled: true
global:
  storageClass: "rook-ceph-block-2"
#externalDatabase:
#  host: "postgres-postgresql.postgres"
#  port: 5432
#  user: postgres
#  password: "ilQ7lUqd2B"
#  database: keycloak
auth:
  adminUser: user
  adminPassword: "kc@123"
#  tls:
#    enabled: true
#    autoGenerated: true

ingress:
  enabled: true
  tls: true
#  servicePort: https
  hostname: keycloak.devserver.ir
image:
  debug: true
#proxyAddressForwarding: true
#extraEnvVars:
#  - name: PROXY_ADDRESS_FORWARDING
#    value: "true"
extraEnvVars:
  - name: KEYCLOAK_LOG_LEVEL
    value: DEBUG
  - name: KEYCLOAK_PROXY_ADDRESS_FORWARDING
    value: "true"
  - name: KEYCLOAK_FRONTEND_URL
    value: "https://keycloak.devserver.ir"
